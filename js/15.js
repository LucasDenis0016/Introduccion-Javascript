// Metodos Arrays

const numero = [10, 20, 30, 40, 50];


const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo'];

// Para agregar elemento a un Array :

// numero.push(60);    // elemento que se coloca al final del array
// numero.unshift(5); // elemento que van al principio del array


// Eliminar elementos de un arreglo

// numero.pop();      // elimina el ultimo elemento
// numero.unshift(); // elimina el primer elemento
// numero.splice(2, 1);


// Rest Operator o Spread Operator

// const nuevoArreglo = [... meses, 'junio']; // El elemento se coloca ultimo 
// const nuevoArreglo = ['junio', ...meses];    // El elemento se coloca Primero
console.log(nuevoArreglo);